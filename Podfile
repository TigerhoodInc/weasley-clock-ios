# Uncomment the next line to define a global platform for your project
 platform :ios, '11.4'

target 'weasley-clock' do
  use_frameworks!

  pod 'Firebase/Auth'

  target 'weasley-clockTests' do
    inherit! :search_paths
  end
end
